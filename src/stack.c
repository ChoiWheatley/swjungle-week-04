#include "stack.h"

bool stack_full(const Stack* stack) { return false; }

bool stack_empty(const Stack* stack) { return false; }

void stack_push(Stack* stack, node_t* new) {}

node_t* stack_pop(Stack* stack) { return NULL; }
